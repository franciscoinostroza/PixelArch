generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Cliente {
  id               String   @id @default(cuid())
  clerkUserId      String   @unique
  paddleCustomerId  String?  @unique
  nombre           String
  email            String   @unique
  empresa          String?
  telefono         String?
  activo           Boolean  @default(true)
  creadoEn         DateTime @default(now())
  actualizadoEn    DateTime @updatedAt

  suscripciones Suscripcion[]
  pagos         Pago[]
}

model Servicio {
  id                         String    @id @default(cuid())
  nombre                     String
  descripcion                String
  icono                      String?
  tags                       String[]

  precioUnico                Int
  precioBasico               Int
  precioMantenimiento        Int

  paddlePriceIdUnico         String    @unique
  paddlePriceIdBasico        String    @unique
  paddlePriceIdMantenimiento String    @unique

  activo                     Boolean   @default(true)
  orden                      Int       @default(0)

  suscripciones Suscripcion[]
}

model Suscripcion {
  id                      String      @id @default(cuid())
  clienteId               String
  servicioId              String
  plan                    PlanTipo
  estado                  Estado
  paddleSubscriptionId    String?     @unique
  paddleTransactionId     String?
  deploymentId            String?
  deploymentPlatform      String?
  proximoPago             DateTime?
  entregadoEn             DateTime?
  canceladoEn             DateTime?
  creadoEn                DateTime    @default(now())
  actualizadoEn           DateTime    @updatedAt

  cliente  Cliente  @relation(fields: [clienteId], references: [id])
  servicio Servicio @relation(fields: [servicioId], references: [id])
  pagos    Pago[]
}

enum PlanTipo {
  UNICO
  BASICO
  MANTENIMIENTO
}

enum Estado {
  PENDING
  READY
  ACTIVE
  PAST_DUE
  PAUSED
  CANCELED
}

model Pago {
  id              String     @id @default(cuid())
  clienteId       String
  suscripcionId   String?
  paddleTransactionId String     @unique
  monto           Int
  moneda          String     @default("usd")
  estadoPago      EstadoPago
  creadoEn        DateTime   @default(now())

  cliente     Cliente     @relation(fields: [clienteId], references: [id])
  suscripcion Suscripcion? @relation(fields: [suscripcionId], references: [id])
}

enum EstadoPago {
  SUCCEEDED
  FAILED
  REFUNDED
  PENDING
}
