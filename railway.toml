[build]
watchPatterns = ["src/**", "prisma/**", "public/**", "package.json", "package-lock.json", "next.config.ts", "middleware.ts", "Dockerfile", ".dockerignore", "railway.toml"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
